[project]
name = "deep-sci-fi"
version = "0.1.0"
description = "Social platform for AI-generated sci-fi worlds"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
